# BiChat Messenger — FastAPI backend dependencies

# Core framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0   # includes uvloop + httptools for production
starlette>=0.37.0            # SessionMiddleware, StaticFiles, etc.

# Session (signed cookie, Starlette built-in)
itsdangerous>=2.1.2          # used internally by SessionMiddleware

# Template rendering (Jinja2 for existing HTML templates)
jinja2>=3.1.4

# File uploads
python-multipart>=0.0.9

# Crypto (existing)
cryptography>=42.0.0

# Pydantic v2 (FastAPI uses it natively)
pydantic>=2.7.0

# Optional: load .env
python-dotenv>=1.0.0

# Optional: faster JSON
orjson>=3.10.0

# Dev / testing
httpx>=0.27.0                # async test client for FastAPI
pytest>=8.2.0
pytest-asyncio>=0.23.0